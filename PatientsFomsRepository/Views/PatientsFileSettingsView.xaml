<UserControl x:Class="PatientsFomsRepository.Views.PatientsFileSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PatientsFomsRepository.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary  Source="/PatientsFomsRepository;component/Views/Resources/DefaultStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="Настройки файла пациентов" Style="{StaticResource TextBlockHeader}"/>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Путь к файлу"/>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" >
            <TextBox Width="350"  />
            <Button Content="Выбрать"/>
        </StackPanel>
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Применить форматирование"/>
        <CheckBox  Grid.Row="2" Grid.Column="1" Name="CheckBox1"/>
        <DataGrid  Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" MinHeight="300" MinWidth="500" HorizontalAlignment="Stretch"  Visibility="{Binding IsChecked, ElementName=CheckBox1}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название столбца" Binding="{Binding Path=name}"/>
                <DataGridTextColumn Header="Понятное название" Binding="{Binding Path=altName}"/>
                <DataGridCheckBoxColumn Header="Скрыть" Binding="{Binding Path=hide}"/>
                <DataGridCheckBoxColumn Header="Удалить" Binding="{Binding Path=delete}"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" >
            <Button Content="Сохранить"/>
            <Button Content="Отмена"/>
        </StackPanel>
    </Grid>
</UserControl>
